package com.example.meatshop.enums;

// enum for animal type. Basically just meat/fish types.
// If we ever expand into marinades or spice mixes (or any other products), we can add them here.
public enum AnimalType {
    BEEF, PORK, CHICKEN, LAMB, DUCK, FISH, OTHER
}
